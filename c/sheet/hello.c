
#include <string.h>
#include <math.h>
#include <stdio.h>
int main()
{
    // const char *list = {"-o abcde"};
    // if (strncmp(list, "-o", 2) == 0)
    // {
    //     printf("-Werror: Make all warnings into errors.\n");
    //     const char *value = list + 2;
    //     printf("%s", value);
    // }

    long long a;
    a = pow(2, 2022);
    printf('%lld', a);

    
}